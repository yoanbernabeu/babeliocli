version: "2"

linters:
  enable:
    - bodyclose
    - misspell
    - nolintlint
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
    nolintlint:
      require-explanation: true

formatters:
  enable:
    - gofmt
